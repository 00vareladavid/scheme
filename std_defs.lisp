;;This is the standard library
(define not (lambda (x) (if x #f #t)))

;;  ;(def (not x)
;;    ;(if x #f #t))
;;  
;;  
;;  ;;note pop! could be a library macro, but I need to implement macros first!
;;  
;;  ;;another comment
;;  (define x 100)
;;  (define foo (lambda (x) (+ x 1)))
